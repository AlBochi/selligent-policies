id: "bad-regex-test"
version: "1.0.0"
description: "This policy has an intentionally broken regex to test validation"
agent_role: "mcp_agent"
tool_id: "file_reader"
allowed_actions:
  - Execute
conditions:
  - !PayloadRegexDeny "[invalid-regex-without-closing-bracket"
priority: 100
is_active: true
